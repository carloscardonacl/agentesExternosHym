import { ColorsaldoDirective } from './colorsaldo.directive';

describe('ColorsaldoDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorsaldoDirective();
    expect(directive).toBeTruthy();
  });
});
