import { ColorsaldoPipe } from './colorsaldo.pipe';

describe('ColorsaldoPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorsaldoPipe();
    expect(pipe).toBeTruthy();
  });
});
